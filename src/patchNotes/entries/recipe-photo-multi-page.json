{
  "message": "Recipe photo import now takes more than one photo, so a cookbook recipe that runs across a page turn can be read as a single recipe instead of just the front page.",
  "date": "2026-09-12"
}
