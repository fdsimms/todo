{
  "message": "New templates now apply their tasks loose by default, instead of automatically bundling them into a stack. Pick \"A stack\" under a template's \"When applied\" setting if you want that back.",
  "date": "2026-08-12"
}
